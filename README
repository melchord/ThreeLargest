Find Three Largest Numbers

Write a function that takes in an array of integers and returns a sorted array of the 
three largest integers in the input array. Note that the function should return duplicate
integers if necessary; for example, it should return [10, 10, 12] for an input array of 
[10,5, 9, 10, 12].

Sample input:[141, 1, 17, -7, -17, -27, 18,541, 8, 7, 7]
Sample output: [18, 141, 541]

To run the test class

1. Compile the classes using 
    javac -cp <class path of junit4-4.12.jar>:. test/Tests.java source/TwoSum.java
2. Run the test class
    java -cp <class path of junit4-4.12.jar>:. org.junit.runner.JUnitCore tests.Tests
